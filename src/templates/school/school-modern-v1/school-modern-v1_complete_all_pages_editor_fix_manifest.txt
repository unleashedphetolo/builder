school-modern-v1 complete all-pages section editor fix

Updated:
- App.jsx
- Non-home page files with BuilderSectionTarget fallback guard
- components/governance/SGB.jsx
- components/home/WallOfFame.jsx
- pages/Contact.jsx

What changed:
- Home page behavior was left unchanged.
- Contact page was included and now follows the same fallback title logic.
- App.jsx now creates exact per-page virtual section records when the database section is missing or stale.
- All inner pages receive their own page section object instead of wrong/wide sections.
- Inner page editor wrappers remain visible in builder mode even when a section record was not loaded yet.
- Every inner page now uses exact editor identity such as school_academics, school_sports, school_resources, school_apply_online, school_robotics, school_contact, etc.
- This prevents wrong Notice Board content from controlling other pages and gives the editor a correct section type to open.

No styling/design changes were made.
