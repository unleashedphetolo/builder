Builder 19 updated v2

Targeted builder-only updates:
- Prevents repeated editable fields when alias fields (Body/Text/Summary/Description/Items/Content) contain the same value.
- Keeps duplicate alias values internally synced when the visible alias field is edited, so existing template fallbacks/renderers are not disturbed.
- Keeps template-backed pages aligned to their own template fallback order and fields.
- Keeps date/time controls limited to intentional schedule fields such as startAt/endAt/publishedAt/scheduledAt.
- Adds safe date-only and time-only input support only when the saved value is already in picker-compatible ISO date or time format.
- Adds section-level body/heading font size and font weight controls alongside colour and typography controls.
- Shows the current page and current section in section settings.
- Shows the current preview page in the website theme panel.
- Adds section font-size/font-weight CSS variables in the builder section target without changing template structure.

No template layout or page fallback data was removed.
