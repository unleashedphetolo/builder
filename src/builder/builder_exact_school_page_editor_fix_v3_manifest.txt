Builder exact school page editor fix v3

Updated only the builder editor fallback path for these remaining school pages:
- Campus Facilities
- Attendance Policy
- Student Daily Bulletin

What this keeps/fixes:
- Editor Field Order remains hidden from the editor.
- Editor Field Labels remains hidden from the editor.
- Editor Fallback Content remains hidden from the editor.
- Campus Facilities uses the full template media/list fallback fields.
- Attendance Policy uses the exact page fields in the original template order.
- Student Daily Bulletin uses complete exact fallback fields including footer note.
- These pages are forced into the same template-field editor path as the working pages instead of the generic Services/Policy/Notices editors.
- Boolean dynamic item fields such as urgent are rendered as switches instead of text inputs.
